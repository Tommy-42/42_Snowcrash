# running the exec
```
level13@BornToSec:~$ ./level13
UID 2013 started us but we we expect 4242
```

# lets check the source file
```
...
.LC0:
	.string	"UID %d started us but we we expect %d\n"
.LC1:
	.string	"your token is %s\n"
	.text
	.globl	main
	.type	main, @function
main:
.LFB0:
...
	call	getuid
	cmpl	$4242, %eax
	je	.L2
	call	getuid
...
```

# we can see that he use getuid and compare the return to 4242
# getuid can easely be bypass
# we just need to change the value of $eax on coparaison to get the flag
# we just need to use gdb
```
gdb level13
disass main
	0x0804859a <+14>:	cmp    $0x1092,%eax
break on 0x0804859a ( before the cmp )
r ( run the programm )
set $eax=4242
c ( continue )
```

# we got
```
(gdb) c
Continuing.
your token is 2A31L79asukciNyi8uppkEuSx
[Inferior 1 (process 25825) exited with code 050]
```